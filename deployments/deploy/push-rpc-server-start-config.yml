apiVersion: v1
kind: ConfigMap
metadata:
  name: push-rpc-server-start-config
data:
  discovery.yml: |
    serviceBinaries:
      openim-push: 1
    toolBinaries:
      - check-free-memory
      - check-component
      - seq
    maxFileDescriptors: 10000