apiVersion: v1
kind: ConfigMap
metadata:
  name: msg-rpc-server-start-config
data:
  discovery.yml: |
    serviceBinaries:
      openim-rpc-msg: 1
    toolBinaries:
      - check-free-memory
      - check-component
      - seq
    maxFileDescriptors: 10000