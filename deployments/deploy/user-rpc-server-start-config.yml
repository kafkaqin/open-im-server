apiVersion: v1
kind: ConfigMap
metadata:
  name: user-rpc-server-start-config
data:
  discovery.yml: |
    serviceBinaries:
      openim-rpc-user: 1
    toolBinaries:
      - check-free-memory
      - check-component
      - seq
    maxFileDescriptors: 10000