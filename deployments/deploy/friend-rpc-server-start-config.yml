apiVersion: v1
kind: ConfigMap
metadata:
  name: friend-rpc-server-start-config
data:
  discovery.yml: |
    serviceBinaries:
      openim-rpc-friend: 1
    toolBinaries:
      - check-free-memory
      - check-component
      - seq
    maxFileDescriptors: 10000