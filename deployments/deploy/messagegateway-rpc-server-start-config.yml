apiVersion: v1
kind: ConfigMap
metadata:
  name: messagegateway-rpc-server-start-config
data:
  discovery.yml: |
    serviceBinaries:
      openim-msggateway: 1
    toolBinaries:
      - check-free-memory
      - check-component
      - seq
    maxFileDescriptors: 10000